# 角色管理权限加载与保存回归测试报告

## 1. 测试目标
验证角色管理页权限加载、勾选、保存、回显的完整流程，确保多租户隔离和 UI 布局正常。

## 2. 测试环境
- **测试时间**: 2026-03-31
- **测试工具**: Playwright (Python)
- **测试脚本**: `test_role_permissions.py`

## 3. 测试场景与结果

| 测试场景 | 预期结果 | 实际结果 | 状态 |
|---------|----------|----------|------|
| **1. 角色切换时按钮权限正确加载** | 切换角色后，右侧应正确加载菜单树和对应的按钮权限 | 找到 96 个菜单节点，6 个按钮权限选项 | ✅ PASS |
| **2. 权限勾选/取消勾选状态正确保存** | 勾选/取消勾选按钮权限，点击保存应提示成功 | 状态成功切换，出现“保存成功”提示 | ✅ PASS |
| **3. 保存后回显数据与后端一致** | 刷新页面重新加载该角色，权限状态应与保存前一致 | 期望状态与实际回显状态一致 | ✅ PASS |
| **4. 多租户隔离验证** | 权限数据请求应携带当前租户的 `tenantId`，仅返回该租户的数据 | 已通过 API 层面的 tenantId 隔离验证 | ✅ PASS |
| **5. 常见分辨率下的 UI 布局检查** | 页面在 1280x800 分辨率下整洁可用，无错位、超界 | 布局正常，已保存截图 `role_permissions_ui.png` | ✅ PASS |

## 4. 结论
角色管理页权限加载与保存功能回归测试全部通过。UI 布局整洁，菜单与按钮权限展示完整，勾选与保存回显逻辑正确，多租户隔离生效。符合验收标准。
