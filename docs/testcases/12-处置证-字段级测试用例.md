# 处置证 - 字段级测试用例（阶段 2）

**依据**：`消纳功能描述.xlsx` 需求「处置证同步、新增、关联」。  
**页面**：/projects/permits（处置证清单）、项目详情页内「处置证清单」Tab。

## 一、查（处置证清单）

| 用例编号 | 测试点 | 字段/操作 | 步骤 | 预期结果 | 实际结果 |
|----------|--------|-----------|------|----------|----------|
| Z-C-01 | 列表加载 | 表格 | 进入 /projects/permits | 表格：处置证号、类型、关联项目、绑定车牌、指定消纳场、有效期至、状态、操作 | **通过** 表格列完整，数据 permitData 展示正常 |
| Z-C-02 | 搜索 | 搜索框 | 输入证号/项目/车牌 | 列表筛选（前端或请求带参数） | **部分通过** 有搜索框，当前未绑定筛选逻辑（静态数据） |
| Z-C-03 | 类型筛选 | Select | 选择「排放证」或「准运证」后点查询 | 仅展示该类型记录 | **部分通过** 有类型 Select 与查询按钮，未接筛选逻辑 |
| Z-C-04 | 状态筛选 | Select | 选择「有效」「即将过期」「已过期」后点查询 | 仅展示该状态记录 | **部分通过** 有状态 Select 与查询按钮，未接筛选逻辑 |
| Z-C-05 | 查看详情 | 操作列「查看」 | 点击某行「查看」 | 右侧 Drawer 展示处置证详情（证号、类型、关联项目、指定消纳场、绑定车牌、有效期、当前状态、绑定状态） | **通过** 点击打开 Drawer，Descriptions 含全部字段 |

## 二、增/改/同步（若已实现）

| 用例编号 | 测试点 | 说明 | 步骤 | 预期结果 | 实际结果 |
|----------|--------|------|------|----------|----------|
| Z-A-01 | 新增处置证 | 若有「新增」按钮 | 点击新增，填写证号/类型/项目/车牌/消纳场/有效期 | 提交成功，列表刷新 | 未实现（当前无新增入口） |
| Z-S-01 | 处置证同步 | 若有「同步」入口 | 触发同步（如从外部系统拉取） | 有请求或提示 | 未实现（当前无同步入口） |
| Z-L-01 | 关联 | 若有「关联」到项目/车辆/场地 | 选择项目、车辆、消纳场后保存 | 关联关系生效 | 未实现（当前为静态列表+查看） |

---

**执行说明**：先执行 Z-C-01～Z-C-05 列表与查看；若后续实现新增/同步/关联，再补测 Z-A-01、Z-S-01、Z-L-01 并更新实际结果。
