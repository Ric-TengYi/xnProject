# 车辆管理技术方案

## 1. 模块建设目标
建立覆盖车辆、维修、维保、保险、调度、油卡、人证、财务和报表的车队运营体系。

## 2. 模块技术范围
车辆管理模块技术范围覆盖前端页面、后台接口、业务服务、数据模型、审批联动、消息通知、日志审计、导入导出、移动端或第三方对接等能力。

## 3. 总体能力设计
车辆管理模块是平台运输侧经营管理中心，需要同时满足车辆全生命周期档案管理、调度管理、成本管理和安全管理要求。技术上采用统一前后端分层架构，前端负责界面编排与交互约束，后端负责业务校验、流程编排、数据存储、异步任务和接口开放。

## 4. 功能点技术实现方案

### 4.1 车辆信息

**目标能力**
建设“车辆信息”的标准化服务能力，满足车辆类别（油车/电车）、品牌、型号、参数、轴数、自重、载重等所有车辆相关信息的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“车辆信息”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“车辆信息”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.2 保险管理

**目标能力**
建设“保险管理”的标准化服务能力，满足管理公司所有车辆（含租赁）保险信息（如承保单位，险种等），保险金额、起止时间、出险赔付相关信息数据录入，汇总、查询分析，支持自定义时间保险到期预警的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“保险管理”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“保险管理”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.3 维修管理

**目标能力**
建设“维修管理”的标准化服务能力，满足所有公司名下的车辆维修维护保养情况统计，日常车辆机务管理，定点维修企业信息统计等，相关费用登记汇总查询。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“维修管理”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“维修管理”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.4 维保计划

**目标能力**
建设“维保计划”的标准化服务能力，满足管理人员根据车辆维保记录，制订车辆的维保计划，主要功能为维保计划的新增、修改、删除、维保登记、导出、查看历史维保记录等。支持自定义日月年维保到期预警提示。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“维保计划”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles`：承载“维保计划”相关的查询、提交、处理或结果获取能力。
- `PUT /api/vehicles/{id}`：承载“维保计划”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.5 维保信息录入

**目标能力**
建设“维保信息录入”的标准化服务能力，满足全面记录车辆维修保养过程中的关键信息，确保维修过程透明、高效，并为后续保养及故障排查提供有力依据。该登记表内容需详尽，结构清晰，覆盖车辆基本信息、维修情况、维保项目、费用明细及维修人员签字等多个方面。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“维保信息录入”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“维保信息录入”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.6 维修申请

**目标能力**
建设“维修申请”的标准化服务能力，满足车辆维护人员根据车辆使用情况（里程、年限）做出车辆维修申请计划。具有提出申请、修改、删除、导出、查询等功能。主要信息包括车牌号、车型、购置日期、行驶里程、维修原因、维修内容、费用预算、紧急程度、申请人、申请日期、审核状态等。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `POST /api/vehicles`：承载“维修申请”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles/{id}/submit`：承载“维修申请”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“维修申请”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.7 维修审批

**目标能力**
建设“维修审批”的标准化服务能力，满足管理人员对提交的车辆维修申请，根据车辆实际情况，进行审批。具有审核、驳回、查询、导出等功能。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `POST /api/vehicles/{id}/submit`：承载“维修审批”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles/{id}/approve`：承载“维修审批”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles/{id}/reject`：承载“维修审批”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.8 车型管理

**目标能力**
建设“车型管理”的标准化服务能力，满足对车辆的车型分类管理，形成车型字典库。主要功能有新增、修改、删除等功能。主要信息包括品牌、车型、限乘人数、载重数等。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“车型管理”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“车型管理”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.9 车辆信息维护

**目标能力**
建设“车辆信息维护”的标准化服务能力，满足管理人员维护车辆的基础信息，主要功能有新增、修改、删除等。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“车辆信息维护”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“车辆信息维护”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.10 车队信息查询

**目标能力**
建设“车队信息查询”的标准化服务能力，满足分区块直观展示各车队的信息，包括车队名、队长、司机及状态，并可快捷填写出勤和排班信息。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“车队信息查询”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“车队信息查询”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.11 车队信息维护

**目标能力**
建设“车队信息维护”的标准化服务能力，满足对车队以及司机信息维护管理，主要功能有录入、修改、删除、查看等操作。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“车队信息维护”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“车队信息维护”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.12 运输计划

**目标能力**
建设“运输计划”的标准化服务能力，满足制定车队运输计划，主要功能为运输计划的新增、修改、删除、导出、查看采购计划和物资运输计划等功能。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“运输计划”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles`：承载“运输计划”相关的查询、提交、处理或结果获取能力。
- `PUT /api/vehicles/{id}`：承载“运输计划”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.13 车队调度申请

**目标能力**
建设“车队调度申请”的标准化服务能力，满足根据车队运输计划，调度人员申请车队调拨。调度人员根据运输计划，提前进行车队调拨申请。主要功能有提出申请、修改、删除、导出、查询等功能。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `POST /api/vehicles`：承载“车队调度申请”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles/{id}/submit`：承载“车队调度申请”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“车队调度申请”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.14 车队调度审批

**目标能力**
建设“车队调度审批”的标准化服务能力，满足管理人员对提交的车队调拨申请，根据实际需求，进行审批。具有审核、驳回、查询、导出等功能。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `POST /api/vehicles/{id}/submit`：承载“车队调度审批”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles/{id}/approve`：承载“车队调度审批”相关的查询、提交、处理或结果获取能力。
- `POST /api/vehicles/{id}/reject`：承载“车队调度审批”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.15 油（电）卡管理

**目标能力**
建设“油（电）卡管理”的标准化服务能力，满足管理人员对油（电）卡实现统一管理，配属到车辆，进行一车一卡管理。主要功能有新增、修改、删除、余额统计、用油确认、查看等记录、统计功能。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“油（电）卡管理”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“油（电）卡管理”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.16 人证管理

**目标能力**
建设“人证管理”的标准化服务能力，满足相关人员的信息管理（人员信息、驾驶证、营运证）及相关费用管理。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“人证管理”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“人证管理”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.17 财务管理

**目标能力**
建设“财务管理”的标准化服务能力，满足根据运输合同管理相关合同款项的结算情况。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“财务管理”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“财务管理”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

### 4.18 报表管理

**目标能力**
建设“报表管理”的标准化服务能力，满足单车人员工资、成本、费用、利润报表，整体成本、费用利润报表。的业务目标，并为后续审批、预警、统计、移动端和第三方联动提供统一接口支撑。

**系统提供能力说明**
系统需提供主数据维护、状态流转、日志留痕、接口开放、消息通知和统计汇总等能力，并支持按组织、项目、场地、车辆、合同等维度扩展查询范围。

**参与角色**
- 车队管理员、机务管理员、调度员、财务人员、驾驶员管理人员

**前后端功能组成**
- 前端：管理列表、详情抽屉、表单页签、流程时间线、导入导出弹窗、地图或图表视图。
- 后端：控制器接口、服务编排、规则校验、数据落库、异步任务、日志审计、消息触发。

**业务接口说明**
- `GET /api/vehicles`：承载“报表管理”相关的查询、提交、处理或结果获取能力。
- `GET /api/vehicles/{id}`：承载“报表管理”相关的查询、提交、处理或结果获取能力。

**输入输出参数说明**
- 输入：主业务对象编号、组织范围、时间范围、状态、关键业务参数、附件或现场信息。
- 输出：业务结果状态、结果数据明细、日志编号、流程节点信息、导出或下载结果。

**业务流程说明**
1. 前端页面或移动端提交业务请求，并携带当前用户、组织范围和业务参数。
2. 接口层完成参数校验、权限校验、状态校验和幂等校验。
3. 服务层执行业务规则计算、状态流转、关联对象更新和消息触发。
4. 数据层完成主表、明细表、关联表、日志表和统计快照写入。
5. 异步任务根据需要执行导出、同步、推送、回调、报表汇总或预警生成。

**数据存储与状态流转**
- 主业务数据写入模块主表，明细、附件、流程、消息、日志分别写入关联表或扩展表。
- 状态流转需统一采用状态机控制，至少覆盖“草稿、已提交、处理中、已完成、已退回、已关闭/已作废”等状态组合中的适用状态。

**权限控制**
- 接口级权限控制：按菜单、按钮、角色和组织范围控制访问。
- 数据级权限控制：按用户所属组织、授权场地、授权项目、授权车辆或合同范围过滤结果。

**异常处理机制**
- 接口参数校验失败返回统一错误码与字段定位信息。
- 权限不足或数据越权时直接拒绝，并记录安全审计日志。
- 并发提交或重复处理时通过幂等键、状态机或唯一约束防止重复写入。

**日志与审计要求**
- 记录请求来源、操作人、操作时间、关键参数、前后状态和处理结果。
- 涉及审批、推送、同步、导入导出、删除、解禁等动作时需保留完整审计链。

**可扩展设计**
- 接口层预留版本控制、扩展字段和第三方回调适配能力。
- 数据层预留编码、来源平台、批次号、同步标识和外部对象编号等扩展字段。

## 5. 核心数据对象设计
- 车辆主档 Vehicle
- 车辆保险 VehicleInsurance
- 维修申请 RepairRequest
- 维保计划 MaintenancePlan
- 车队档案 Fleet
- 调度申请 DispatchRequest
- 油电卡 FuelCard
- 人证台账 PersonnelCertificate

## 6. 业务接口设计
- 统一接口前缀建议采用 `/api/vehicles` 或对应移动端、专项前缀，实现前后端接口分层清晰。
- 查询类接口支持分页、排序、条件过滤、字典翻译和状态聚合。
- 变更类接口支持幂等控制、状态校验、附件提交、审批提交流程和消息触发。

## 7. 业务流程设计
- 业务流程统一遵循“发起/采集 -> 校验 -> 提交/处理 -> 审批/同步 -> 结果回写 -> 留痕/通知”的流程骨架。
- 涉及统计、导出、同步、报表和预警生成的场景，采用异步任务和批处理机制降低接口阻塞风险。

## 8. 权限与数据范围控制
- 统一接入组织、用户、角色和数据范围控制体系，按单位、项目、场地、车辆、合同等维度裁剪数据可见范围。
- 移动端、第三方接口和后台接口均需绑定独立的身份凭据和访问授权。

## 9. 异常处理与补偿机制
- 关键接口统一返回错误码、错误信息、追踪编号和建议处理方式。
- 异步任务支持失败重试、批次追踪、人工补录、对账补偿和回滚控制。

## 10. 日志审计与追溯要求
- 记录接口访问日志、业务操作日志、审批日志、同步日志、错误日志和消息推送回执。
- 关键业务对象支持按单号、主键、批次号、外部编码追溯全链路处理过程。

## 11. 与第三方或其他模块协同方式
- 与组织权限体系协同实现统一身份和数据授权。
- 与审批配置、消息中心、日志中心协同实现流程驱动、消息触达和可审计治理。
- 与平台对接、小程序、统计分析、数据看板协同实现跨平台数据共享和能力复用。

## 12. 性能与扩展性要求
- 列表和报表需支持分页、条件索引、聚合优化和异步导出。
- 地图、轨迹、视频、地磅、同步日志等大体量数据建议分表、归档或采用冷热分层策略。

## 13. 本模块交付说明
车辆管理模块应作为独立可交付能力建设，既满足甲方当前招标要求，也为后续扩展第三方对接、移动端深化、规则引擎和经营分析能力保留扩展接口。
