# 智慧渣土消纳管控 - 功能测试报告

**测试时间**：2026-03-16  
**测试环境**：本地 (前端 port 5173, 后端 port 8090)  
**测试方式**：浏览器自动化 + 接口校验  

---

## 一、测试范围与用例依据

- **用例来源**：《测试用例.xlsx》各模块工作表  
- **输入框与校验细化**：《测试用例-输入框与校验说明.md》  
- **CRUD 覆盖**：列表查询、详情、新增、编辑、删除（按模块实现情况）  

---

## 二、环境与前置说明

- **后端**：Spring Boot 8090，依赖 MySQL + Redis。若数据库未启动或未执行 schema/seed，登录等接口可能返回 **503（服务暂时不可用）** 或连接失败。  
- **前端**：Vite 5173，代理 `/api` → 8090。  
- **高德地图**：已在 `index.html` 配置 key；安全密钥仅服务端使用。  
- **登录修复**：已修复“数据库异常时登录返回 500”的问题，改为返回 503 或正常 401/403，便于前端区分“密码错误”与“服务不可用”。  

---

## 三、模块测试结果

### 1. 登录模块（TC107/TC108 及 验证码登录）

| 用例编号 | 类型     | 测试点                     | 结果 | 说明 |
|----------|----------|----------------------------|------|------|
| 正向     | 账号密码登录 | 输入合法账号密码，提交       | 阻塞 | 依赖 DB：当前环境登录接口返回 500/503，需确保 MySQL 已启动且执行 `db/schema` 及 seed（如 004、006）后再测。 |
| 异常     | 空提交   | 不填账号/密码直接点登录     | 通过 | 前端校验：账号、密码输入框变为 invalid，符合预期。 |
| 异常     | 错误密码 | 正确账号+错误密码           | 待测 | 需 DB 正常时验证返回 401 及统一错误结构。 |
| 验证码 Tab | 手机号校验 | 输入非 1[3-9] 开头的 11 位  | 未测 | 前端有 `pattern` 校验，可手工验证。 |

**结论**：前端必填与格式校验正常；登录成功链路需在数据库可用后重测。  

---

### 2. 合同与结算 - 合同清单（TC001/TC002）

| 用例编号 | 类型 | 测试点           | 结果 | 说明 |
|----------|------|------------------|------|------|
| 正向     | 列表 | 进入合同清单页，有筛选与分页 | 通过 | 在有效 session 下访问 `/contracts`，页面展示正常：搜索框「搜索合同编号或项目名称」、开始/结束日期、状态筛选、按钮「发起新合同审批」「导出月度报表」。 |
| 异常     | 未授权/非法参数 | 未登录或越界分页   | 未测 | 需登录后清除 token 再访问，或传非法 pageNo/pageSize 验证接口返回非 500。 |

**CRUD**：  
- **查**：列表页 UI 具备，接口需 DB 正常后验证返回结构。  
- **增/改/删**：依赖「发起新合同」「编辑」等入口及后端接口，未在本轮逐项执行。  

---

### 3. 合同与结算 - 合同详情、入账、结算、月报

- **合同详情（TC003/TC004）**：路由 `/contracts/:id` 存在，详情为前端模拟数据 + 入账记录接口；需在 DB 正常后验证入账接口与详情拉取。  
- **合同入账 / 结算管理 / 月报统计**：对应路由与页面存在，列表/筛选/表单（如交款金额、日期）已按《测试用例-输入框与校验说明.md》整理；接口与 DB 依赖未在本轮逐条执行。  

---

### 4. 消纳场地（TC025–TC034）

- **场地列表**：路由 `/sites`，搜索「场地名称或地址」、列表与筛选与用例一致。  
- **场地详情/基础信息/场地资料/消纳清单/场地创建**：页面或入口存在，输入框限制见《测试用例-输入框与校验说明.md》；接口与 DB 依赖未在本轮逐条执行。  

---

### 5. 单位管理 / 组织管理（TC011–TC018、TC059–TC062）

- **组织列表/组织树**：路由 `/settings/organization`，搜索「组织」「姓名/账号」与用例一致。  
- **单位/建设单位/施工单位/运输单位**：按测试用例为租户类型筛选或占位；需接口与数据就绪后验证。  

---

### 6. 项目管理（TC019–TC024）

- **项目清单/合同清单/场地清单**：路由 `/projects` 等，搜索与筛选与用例一致；接口为占位或部分实现，需 DB 后验证。  

---

### 7. 车辆与运力（TC109–TC118）

- **车辆管理/车队/卡片/追踪/违规**：路由 `/vehicles`、`/vehicles/fleet` 等存在，表单与筛选见输入框说明文档；接口部分为骨架，需 DB 后验证。  

---

### 8. 预警与安全（TC043–TC052、事件/违规等）

- **预警配置**：InputNumber 范围（如容量 50–100、偏航 50–1000）已文档化。  
- **事件管理/违规车辆**：页面与入口存在，接口为占位或骨架。  

---

### 9. 系统设置 - 组织人员、角色、字典、审批、日志（TC059–TC090）

- **角色管理**：角色列表、权限树、保存权限等已实现，需登录后逐项操作验证。  
- **组织人员**：人员列表、新增（必填项）见输入框说明。  
- **数据字典/审核审批配置/系统日志**：列表与筛选存在，接口部分为空或骨架。  

---

### 10. 数据看板与地图（TC087–TC090、地图）

- **看板**：消纳场数据、项目数据为固定/汇总占位。  
- **地图展示**：已接入高德 JS API（key 已配置），`DashboardMap` 在 `window.AMap` 存在时初始化地图容器；安全密钥仅服务端使用。  

---

## 四、已修复问题

1. **登录接口在数据库异常时返回 500**  
   - **修改**：`AuthController.login` 中对 `DataAccessException` 捕获并返回 503；`saveLoginLog` 改为通过 `saveLoginLogSafe` 调用，写日志失败不影响登录响应。  
   - **效果**：DB 不可用时返回 503（服务暂时不可用）；用户名/密码错误仍为 401；用户禁用为 403。  

---

## 五、建议后续操作

1. **环境**：确认 MySQL、Redis 已启动，执行 `db/schema` 及 seed（如 004、006），保证 demo/admin 账号存在。  
2. **登录**：使用 admin/admin 或 demo/对应密码（见 seed 说明）验证 TC107/TC108；验证 503 时前端提示“服务暂时不可用”。  
3. **按模块补测**：在登录通过后，按《测试用例.xlsx》与《测试用例-输入框与校验说明.md》对合同、场地、项目、车辆、组织、角色、预警、日志等逐项执行正向/异常用例，并记录通过率与缺陷。  
4. **CRUD 闭环**：对已实现增删改的模块（如角色权限、人员新增、合同入账等）执行完整增→查→改→删流程并记录结果。  

---

## 六、测试报告汇总

| 模块           | 用例数（约） | 前端/页面 | 接口/DB 依赖 | 状态       |
|----------------|--------------|-----------|--------------|------------|
| 登录           | 2            | 校验通过  | 阻塞         | 待 DB 后重测 |
| 合同与结算     | 10           | 页面正常  | 部分占位     | 待接口验证 |
| 消纳场地       | 10           | 页面正常  | 部分占位     | 待接口验证 |
| 单位/组织管理  | 12           | 页面正常  | 部分实现     | 待接口验证 |
| 项目管理       | 6            | 页面正常  | 占位         | 待接口验证 |
| 车辆与运力     | 10           | 页面正常  | 骨架         | 待接口验证 |
| 预警与安全     | 10+          | 页面正常  | 占位         | 待接口验证 |
| 系统设置       | 20+          | 页面正常  | 部分实现     | 待接口验证 |
| 数据看板/地图  | 4 + 地图     | 地图已接  | 汇总占位     | 待数据验证 |

**说明**：模块测试在“登录通过 + DB 可用”后可继续逐模块执行并更新本报告；当前阶段以环境修复与登录验证为优先。
